{
  "name": "qwen-dap-mcp",
  "version": "0.18.0",
  "displayName": "Qwen DAP MCP",
  "description": "Agentic native runtime debugging for Qwen Code through a hardened local DAP-to-MCP bridge, with crash/hang diagnosis, runtime forensics, rr workflows and a bundled Skill.",
  "mcpServers": {
    "qwen-dap-mcp": {
      "command": "node",
      "args": [
        "${extensionPath}${/}dist${/}index.js"
      ],
      "cwd": "${extensionPath}"
    }
  }
}
