{
  "name": "qwen-dap-mcp",
  "version": "0.18.1",
  "displayName": "Qwen DAP MCP",
  "description": "Native crash, hang, dump, causal/lifetime, symbol and runtime-forensics debugging for Qwen Code through a local DAP-to-MCP bridge.",
  "mcpServers": {
    "qwen-dap-mcp": {
      "command": "node",
      "args": [
        "${extensionPath}${/}dist${/}index.js"
      ],
      "cwd": "${extensionPath}"
    }
  }
}
